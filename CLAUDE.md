# CLAUDE.md

> **The canonical agent/contributor guide for this repo is [`AGENTS.md`](AGENTS.md).**
> Read it first — it covers project identity, architecture, the module DAG,
> conventions, the development workflow, and what not to do. This file restates the
> rules most likely to trip up an AI agent.

## Git commit conventions

Commit messages **must not** contain a `Co-Authored-By:` trailer or any
tool-generated byline (e.g. "Generated with Claude Code"). Keep messages plain: a
concise summary line plus an optional body explaining the *why*. See
[`AGENTS.md` §6](AGENTS.md) (Git commit conventions) and the §7 "What NOT to Do" table.

## Code review dispositions

Every review comment (human, CodeRabbit, or agent) must end up either **fixed in
the PR** or **captured in a GitHub issue** before the PR merges — a finding must
never silently vanish into a merged PR thread. Per comment: `fix-now` → fix +
reply; `already-addressed` → reply with the code ref; `valid-defer` → open a
tracking issue (`deferred-review` label) and reply `📌 Tracked in #N`;
`invalid`/`wont-fix` → record in a (closed-as-not-planned) decision-record issue
and reply with the rationale. Always reply on the originating thread. Full detail
in [`AGENTS.md` §6](AGENTS.md) (Code review dispositions).

## Quick reminders (full detail in AGENTS.md)

- **Module DAG:** `core/ → bridge/ → audio/ → api/ → lib.rs`. No reverse dependencies.
- **RT-safety:** never allocate, lock, or block on the OS audio callback thread. Use
  `BridgeProducer::push_samples_or_drop()` (alloc-free in steady state via the free-list
  return ring — see [`docs/designs/0001-rt-allocation-guarantee.md`](docs/designs/0001-rt-allocation-guarantee.md)).
- **All backends** go through `BridgeStream<S>` + the `PlatformStream` trait.
- **Errors:** every `AudioError` variant needs an `ErrorKind` and a `Recoverability`
  classification. No panics in library code.
- **Capabilities:** never claim a platform supports a feature it doesn't — gate via
  `PlatformCapabilities`.
- **Architecture decisions** are recorded as ADRs in [`docs/designs/`](docs/designs/).
